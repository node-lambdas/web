{
  "name": "jsfn-run",
  "private": true,
  "type": "module",
  "prettier": "@cloud-cli/prettier-config",
  "zmain": "./server/index.mjs",
  "scripts": {
    "ci": "npm i --no-fund --no-audit && npm run build",
    "build": "vue-tsc && vite build --assetsInlineLimit=0",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@cloud-cli/prettier-config": "^1.0.0",
    "typescript": "^5.3.3",
    "vite": "^4.5.2"
  },
  "dependencies": {
    "@homebots/parse-html": "^1.1.1",
    "es-eventdelegate": "^1.0.0"
  }
}
